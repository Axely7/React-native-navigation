import {AuthState} from './AuthContext';

export const authReducer = (state: AuthState, action: any): AuthState => {
  switch (key) {
    case value:
      break;

    default:
      return state;
  }
};
